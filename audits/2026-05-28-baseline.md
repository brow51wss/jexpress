# CODE SLOP & SECURITY AUDIT REPORT

- **Project:** jexpress
- **Project path:** `/Users/marlonsarmiento/Desktop/jexpress`
- **Project fingerprint:** `f65a46654b489826`
- **Scanned:** 2026-05-28T15:23:09
- **Trigger:** baseline
- **Git HEAD:** 5eafa83e (branch: `sentinel`) · ⚠️ uncommitted changes present
- **Sensitive fields audited:** access_token, card_number, contactNumber, email, error_context, event_payloads, fullName, password, phone, secret, sessionToken, userId, user_id
- **Files scanned:** 174
- **project-context.md:** loaded from `/Users/marlonsarmiento/Desktop/jexpress/project-context.md` (6 fields, 3 roles, 6 compliance items, 7 high-risk features)
- **Generated by:** Sentinel MCP (deterministic Pass 2 + LLM judgment layer)

---

## Summary
- CRITICAL: 5
- HIGH: 43
- MEDIUM: 24
- Carry-over from prior audits: 0
- Awaiting LLM judgment (see "Pending Manual Review" below): 120

---

## CARRY-OVER FROM PRIOR AUDITS

These findings appeared in earlier audits and are still present in the current codebase
(based on excerpt grep). The calling LLM should verify whether they are actually unresolved
or whether the excerpt match is a false positive (e.g., the line moved to a different file).

_No carry-over detected._


---

## CRITICAL ISSUES
### .gitignore does not cover .env.production
- **File:** `.gitignore`
- **Rule:** `secrets.env_not_ignored`
- **Excerpt:** `(see .gitignore contents)`
- **Tell Cursor:** Add the following entries to .gitignore: .env.production. Then run `git ls-files | grep -E '\.env'` to confirm no .env file is currently tracked. If any are tracked, remove them from history with `git rm --cached` and rotate the secrets they contained.

### Use of `dangerouslySetInnerHTML`
- **File:** `app/layout.tsx` (line 122)
- **Rule:** `xss.dangerously_set_inner_html`
- **Excerpt:** `dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}`
- **Tell Cursor:** Replace dangerouslySetInnerHTML with safe rendering primitives. If you absolutely must render HTML, sanitize with a library like DOMPurify and document the justification in a code comment.

### Use of `dangerouslySetInnerHTML`
- **File:** `components/analytics-scripts.tsx` (line 12)
- **Rule:** `xss.dangerously_set_inner_html`
- **Excerpt:** `dangerouslySetInnerHTML={{`
- **Tell Cursor:** Replace dangerouslySetInnerHTML with safe rendering primitives. If you absolutely must render HTML, sanitize with a library like DOMPurify and document the justification in a code comment.

### Use of `dangerouslySetInnerHTML`
- **File:** `components/analytics-scripts.tsx` (line 35)
- **Rule:** `xss.dangerously_set_inner_html`
- **Excerpt:** `dangerouslySetInnerHTML={{`
- **Tell Cursor:** Replace dangerouslySetInnerHTML with safe rendering primitives. If you absolutely must render HTML, sanitize with a library like DOMPurify and document the justification in a code comment.

### Use of `dangerouslySetInnerHTML`
- **File:** `components/ui/chart.tsx` (line 83)
- **Rule:** `xss.dangerously_set_inner_html`
- **Excerpt:** `dangerouslySetInnerHTML={{`
- **Tell Cursor:** Replace dangerouslySetInnerHTML with safe rendering primitives. If you absolutely must render HTML, sanitize with a library like DOMPurify and document the justification in a code comment.


## HIGH ISSUES
### Sensitive field `email` written to console.warn
- **File:** `app/api/dashboard/request-otp/route.ts` (line 48)
- **Rule:** `data_exposure.sensitive_log`
- **Excerpt:** `console.warn('[request-otp] No user found for email:', email)`
- **Tell Cursor:** Remove or mask the sensitive value at app/api/dashboard/request-otp/route.ts:48. Sensitive fields must never reach console output, error reporters, or analytics. If logging is needed for debugging, log a redacted summary (e.g., `email.split('@')[1]` for domain-only) or remove entirely.

### Sensitive field `email` written to console.log
- **File:** `app/api/dashboard/request-otp/route.ts` (line 72)
- **Rule:** `data_exposure.sensitive_log`
- **Excerpt:** `console.log('[request-otp] Sending OTP email to:', email)`
- **Tell Cursor:** Remove or mask the sensitive value at app/api/dashboard/request-otp/route.ts:72. Sensitive fields must never reach console output, error reporters, or analytics. If logging is needed for debugging, log a redacted summary (e.g., `email.split('@')[1]` for domain-only) or remove entirely.

### Sensitive field `email` written to console.log
- **File:** `app/api/dashboard/request-otp/route.ts` (line 99)
- **Rule:** `data_exposure.sensitive_log`
- **Excerpt:** `console.log('[request-otp] OTP email sent successfully to:', email)`
- **Tell Cursor:** Remove or mask the sensitive value at app/api/dashboard/request-otp/route.ts:99. Sensitive fields must never reach console output, error reporters, or analytics. If logging is needed for debugging, log a redacted summary (e.g., `email.split('@')[1]` for domain-only) or remove entirely.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/contact-hero.tsx` (line 25)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/profile-leadership.tsx` (line 26)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/profile-leadership.tsx` (line 82)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/whyus-hero.tsx` (line 27)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/contact-cta.tsx` (line 8)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/contact-cta.tsx` (line 83)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/contact.tsx` (line 461)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `xmlns="http://www.w3.org/2000/svg"`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/why-us.tsx` (line 56)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `xmlns="http://www.w3.org/2000/svg"`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/why-us.tsx` (line 190)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `xmlns="http://www.w3.org/2000/svg"`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/services.tsx` (line 52)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `xmlns="http://www.w3.org/2000/svg"`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/services.tsx` (line 183)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `xmlns="http://www.w3.org/2000/svg"`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/leadership-officers.tsx` (line 34)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/leadership-officers.tsx` (line 95)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/services-hero.tsx` (line 26)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/whyus-values.tsx` (line 30)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/whyus-values.tsx` (line 74)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/book-why.tsx` (line 35)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/book-why.tsx` (line 89)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/about-team.tsx` (line 15)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/about-team.tsx` (line 83)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/profile-hero.tsx` (line 26)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/about-hero.tsx` (line 27)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/services-cta.tsx` (line 8)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/services-cta.tsx` (line 60)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/leadership-orgchart.tsx` (line 40)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/leadership-orgchart.tsx` (line 91)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/services-fleet.tsx` (line 26)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="relative z-10 w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/services-fleet.tsx` (line 78)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="relative z-10 w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/profile-fleet.tsx` (line 14)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/profile-fleet.tsx` (line 64)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/leadership-hero.tsx` (line 26)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/book-hero.tsx` (line 27)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/franchise-content.tsx` (line 69)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/franchise-content.tsx` (line 115)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/franchise-content.tsx` (line 269)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/franchise-content.tsx` (line 311)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/franchise-content.tsx` (line 344)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `components/franchise-content.tsx` (line 374)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block relative z-10" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `app/privacy-policy/page.tsx` (line 36)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.

### Hardcoded `http://` URL (insecure transport)
- **File:** `app/terms/page.tsx` (line 36)
- **Rule:** `communication.insecure_http`
- **Excerpt:** `<svg viewBox="0 0 1440 56" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" className="w-full block" style={{ height: 56 }} aria-hidden="true">`
- **Tell Cursor:** Replace http:// with https://. Plain HTTP traffic is readable and modifiable by anyone on the network path. iOS and Android also block plain HTTP by default — this likely doesn't work in production builds.


## MEDIUM ISSUES
### File exceeds 200 lines (429 lines)
- **File:** `components/book-form.tsx` (line 429)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(429 lines total)`
- **Tell Cursor:** Refactor components/book-form.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (242 lines)
- **File:** `components/franchise-form.tsx` (line 242)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(242 lines total)`
- **Tell Cursor:** Refactor components/franchise-form.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (486 lines)
- **File:** `components/contact.tsx` (line 486)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(486 lines total)`
- **Tell Cursor:** Refactor components/contact.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (201 lines)
- **File:** `components/services.tsx` (line 201)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(201 lines total)`
- **Tell Cursor:** Refactor components/services.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (213 lines)
- **File:** `components/footer.tsx` (line 213)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(213 lines total)`
- **Tell Cursor:** Refactor components/footer.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (234 lines)
- **File:** `components/header.tsx` (line 234)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(234 lines total)`
- **Tell Cursor:** Refactor components/header.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (342 lines)
- **File:** `components/contact-form.tsx` (line 342)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(342 lines total)`
- **Tell Cursor:** Refactor components/contact-form.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (384 lines)
- **File:** `components/franchise-content.tsx` (line 384)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(384 lines total)`
- **Tell Cursor:** Refactor components/franchise-content.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (260 lines)
- **File:** `app/privacy-policy/page.tsx` (line 260)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(260 lines total)`
- **Tell Cursor:** Refactor app/privacy-policy/page.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (206 lines)
- **File:** `app/terms/page.tsx` (line 206)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(206 lines total)`
- **Tell Cursor:** Refactor app/terms/page.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (351 lines)
- **File:** `components/ui/chart.tsx` (line 351)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(351 lines total)`
- **Tell Cursor:** Refactor components/ui/chart.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (244 lines)
- **File:** `components/ui/field.tsx` (line 244)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(244 lines total)`
- **Tell Cursor:** Refactor components/ui/field.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (213 lines)
- **File:** `components/ui/calendar.tsx` (line 213)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(213 lines total)`
- **Tell Cursor:** Refactor components/ui/calendar.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (276 lines)
- **File:** `components/ui/menubar.tsx` (line 276)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(276 lines total)`
- **Tell Cursor:** Refactor components/ui/menubar.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (726 lines)
- **File:** `components/ui/sidebar.tsx` (line 726)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(726 lines total)`
- **Tell Cursor:** Refactor components/ui/sidebar.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (257 lines)
- **File:** `components/ui/dropdown-menu.tsx` (line 257)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(257 lines total)`
- **Tell Cursor:** Refactor components/ui/dropdown-menu.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (252 lines)
- **File:** `components/ui/context-menu.tsx` (line 252)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(252 lines total)`
- **Tell Cursor:** Refactor components/ui/context-menu.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (241 lines)
- **File:** `components/ui/carousel.tsx` (line 241)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(241 lines total)`
- **Tell Cursor:** Refactor components/ui/carousel.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (554 lines)
- **File:** `components/dashboard/services-manager.tsx` (line 554)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(554 lines total)`
- **Tell Cursor:** Refactor components/dashboard/services-manager.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (293 lines)
- **File:** `components/dashboard/user-management.tsx` (line 293)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(293 lines total)`
- **Tell Cursor:** Refactor components/dashboard/user-management.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### File exceeds 200 lines (229 lines)
- **File:** `components/dashboard/submissions-table.tsx` (line 229)
- **Rule:** `code_quality.file_too_long`
- **Excerpt:** `(229 lines total)`
- **Tell Cursor:** Refactor components/dashboard/submissions-table.tsx into smaller modules. Identify cohesive sections (by responsibility) and extract them into separate files. Aim for 200 lines or fewer per file.

### `npm` not available — supply-chain vulnerability scan skipped
- **File:** `package.json`
- **Rule:** `supply_chain.npm_unavailable`
- **Excerpt:** `(npm command not found)`
- **Tell Cursor:** Install Node.js / npm so Sentinel can run `npm audit` on each scan. Without this, dependency vulnerabilities are not detected by the MCP.

### State-changing HTTP routes detected but no CSRF middleware found
- **File:** `app/api/contact/route.ts`
- **Rule:** `csrf.middleware_absent`
- **Excerpt:** `(no csurf / @fastify/csrf-protection / @nestjs/csrf / similar imported)`
- **Tell Cursor:** If the routes use cookie-based session authentication (browser clients), add CSRF protection. Recommended: `csurf` (Express), `@fastify/csrf-protection` (Fastify), `@nestjs/csrf` (NestJS), Next.js middleware with double-submit cookie pattern. If the routes are JWT-Bearer only (mobile / API), CSRF is not strictly required — in that case, document that fact in code/architecture notes so future contributors don't accidentally introduce cookie auth without CSRF. Severity is MEDIUM because this check cannot tell which auth model you're using.

### Sensitive fields declared but no screenshot/screen-recording protection found
- **File:** `(codebase-wide)`
- **Rule:** `privacy.no_screen_capture_protection`
- **Excerpt:** `sensitive_fields_declared=6; no FLAG_SECURE / expo-screen-capture / similar`
- **Tell Cursor:** Decide whether screens that render sensitive fields need screenshot/recording protection. On Android: set `WindowManager.LayoutParams.FLAG_SECURE` on Activities with sensitive views. On iOS: observe `UIApplication.userDidTakeScreenshotNotification` and/or use `expo-screen-capture` `preventScreenCaptureAsync()` while sensitive screens are mounted. If you've made a deliberate decision NOT to add this (e.g., not banking-grade app), document that decision in `project-context.md` so this finding can be dismissed in future audits.


---

## PENDING MANUAL REVIEW (judgment-laden checks)

The deterministic scanner identifies *where* judgment is needed but cannot make the call alone.
The calling LLM should review the items below and append findings to this report under the
appropriate severity heading above. Items requiring runtime verification (actual HTTP requests,
real device testing, dashboard checks) should be called out separately.

### API routes detected (15 files)
For each route, judge:
- Auth/session check at the top of the handler?
- Admin-only routes verifying user role (not just login)?
- DB queries inside the handler filtering by current user's ID?
- Handler accepting a user ID from request body / URL without verifying session match?
- Server-side input validation present?

#### `app/api/contact/route.ts`
```
import { Resend } from 'resend'
import { NextResponse } from 'next/server'
import { checkRateLimit, getClientIp } from '@/lib/rate-limit'
import { cleanField, isValidEmail, display, LIMITS } from '@/lib/form-utils'
import { createAdminClient } from '@/lib/supabase'

const resend = new Resend(process.env.RESEND_API_KEY)

export async function POST(request: Request) {
  const ip = getClientIp(request)
  if (!checkRateLimit(ip).allowed) {
    return NextResponse.json({ error: 'Too many requests. Please try again later.' }, { status: 429 })
  }

  let body: Record<string, unknown>
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid request.' }, { status: 400 })
  }

  const fullName    = cleanField(body.fullName,    LIMITS.name)
  const email       = cleanField(body.email,       LIMITS.email)
  const phone       = cleanField(body.phone,       LIMITS.phone)
  const agency      = cleanField(body.agency,      LIMITS.short)
```

#### `app/api/inquiry/route.ts`
```
import { Resend } from 'resend'
import { NextResponse } from 'next/server'
import { checkRateLimit, getClientIp } from '@/lib/rate-limit'
import { cleanField, isValidEmail, display, LIMITS } from '@/lib/form-utils'
import { createAdminClient } from '@/lib/supabase'

const resend = new Resend(process.env.RESEND_API_KEY)

export async function POST(request: Request) {
  const ip = getClientIp(request)
  if (!checkRateLimit(ip).allowed) {
    return NextResponse.json({ error: 'Too many requests. Please try again later.' }, { status: 429 })
  }

  let body: Record<string, unknown>
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid request.' }, { status: 400 })
  }

  const fullName      = cleanField(body.fullName,      LIMITS.name)
  const email         = cleanField(body.email,         LIMITS.email)
  const phone         = cleanField(body.phone,         LIMITS.phone)
  const organization  = cleanField(body.organization,  LIMITS.short)
```

#### `app/api/booking/route.ts`
```
import { Resend } from 'resend'
import { NextResponse } from 'next/server'
import { checkRateLimit, getClientIp } from '@/lib/rate-limit'
import { cleanField, isValidEmail, display, LIMITS } from '@/lib/form-utils'
import { createAdminClient } from '@/lib/supabase'

const resend = new Resend(process.env.RESEND_API_KEY)

export async function POST(request: Request) {
  const ip = getClientIp(request)
  if (!checkRateLimit(ip).allowed) {
    return NextResponse.json({ error: 'Too many requests. Please try again later.' }, { status: 429 })
  }

  let body: Record<string, unknown>
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid request.' }, { status: 400 })
  }

  const fullName           = cleanField(body.fullName,           LIMITS.name)
  const companyName        = cleanField(body.companyName,        LIMITS.short)
  const contactNumber      = cleanField(body.contactNumber,      LIMITS.phone)
  const email              = cleanField(body.email,              LIMITS.email)
```

#### `app/api/franchise/route.ts`
```
import { Resend } from 'resend'
import { NextResponse } from 'next/server'
import { checkRateLimit, getClientIp } from '@/lib/rate-limit'
import { cleanField, isValidEmail, display, LIMITS } from '@/lib/form-utils'
import { createAdminClient } from '@/lib/supabase'

const resend = new Resend(process.env.RESEND_API_KEY)

export async function POST(request: Request) {
  const ip = getClientIp(request)
  if (!checkRateLimit(ip).allowed) {
    return NextResponse.json({ error: 'Too many requests. Please try again later.' }, { status: 429 })
  }

  let body: Record<string, unknown>
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid request.' }, { status: 400 })
  }

  const fullName          = cleanField(body.fullName,          LIMITS.name)
  const contactNumber     = cleanField(body.contactNumber,     LIMITS.phone)
  const email             = cleanField(body.email,             LIMITS.email)
  const preferredLocation = cleanField(body.preferredLocation, LIMITS.short)
```

#### `app/api/franchise-auth/route.ts`
```
import { NextResponse } from 'next/server'

async function computeToken(secret: string): Promise<string> {
  const encoder = new TextEncoder()
  const key = await crypto.subtle.importKey(
    'raw',
    encoder.encode(secret),
    { name: 'HMAC', hash: 'SHA-256' },
    false,
    ['sign']
  )
  const sig = await crypto.subtle.sign(
    'HMAC',
    key,
    encoder.encode('franchise_authorized')
  )
  return Array.from(new Uint8Array(sig))
    .map((b) => b.toString(16).padStart(2, '0'))
    .join('')
}

export async function POST(request: Request) {
  try {
    const { password } = await request.json()
    const secret = process.env.FRANCHISE_PASSWORD ?? 'jexpress2026'
```

#### `app/api/services/route.ts`
```
import { NextResponse } from 'next/server'
import { createAdminClient } from '@/lib/supabase'

export const revalidate = 0

export async function GET() {
  const supabase = createAdminClient()

  const { data, error } = await supabase
    .from('services')
    .select(`
      id,
      slug,
      name,
      price,
      price_label,
      icon,
      sort_order,
      is_active,
      service_content (
        section_key,
        description,
        tags
      )
    `)
```

#### `app/api/dashboard/verify-otp/route.ts`
```
import { NextResponse } from 'next/server'
import { createAdminClient } from '@/lib/supabase'
import { cleanField, isValidEmail, LIMITS } from '@/lib/form-utils'
import { createSessionToken, SESSION_COOKIE } from '@/lib/session'

export async function POST(request: Request) {
  let body: Record<string, unknown>
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid request.' }, { status: 400 })
  }

  const email = cleanField(body.email, LIMITS.email)
  const otp   = cleanField(body.otp,   6)

  if (!email || !isValidEmail(email)) {
    return NextResponse.json({ error: 'Invalid email.' }, { status: 400 })
  }
  if (!otp || !/^\d{6}$/.test(otp)) {
    return NextResponse.json({ error: 'Invalid code.' }, { status: 400 })
  }

  const supabase = createAdminClient()

```

#### `app/api/dashboard/request-otp/route.ts`
```
import { Resend } from 'resend'
import { NextResponse } from 'next/server'
import { createAdminClient } from '@/lib/supabase'
import { cleanField, isValidEmail, LIMITS } from '@/lib/form-utils'
import { checkRateLimit, getClientIp } from '@/lib/rate-limit'

const resend = new Resend(process.env.RESEND_API_KEY)

function generateOtp(): string {
  const arr = new Uint32Array(1)
  crypto.getRandomValues(arr)
  return String(100000 + (arr[0] % 900000))
}

export async function POST(request: Request) {
  const ip = getClientIp(request)
  if (!checkRateLimit(ip).allowed) {
    return NextResponse.json({ error: 'Too many requests.' }, { status: 429 })
  }

  let body: Record<string, unknown>
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid request.' }, { status: 400 })
```

#### `app/api/dashboard/logout/route.ts`
```
import { NextResponse } from 'next/server'
import { SESSION_COOKIE } from '@/lib/session'

export async function POST() {
  const response = NextResponse.json({ success: true })
  response.cookies.set(SESSION_COOKIE, '', {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: 0,
    path: '/',
  })
  return response
}
```

#### `app/api/dashboard/groups/route.ts`
```
import { NextResponse } from 'next/server'
import { createAdminClient } from '@/lib/supabase'
import { verifySessionToken, SESSION_COOKIE } from '@/lib/session'
import { cleanField, LIMITS } from '@/lib/form-utils'
import { cookies } from 'next/headers'

async function getSession() {
  const cookieStore = await cookies()
  const token = cookieStore.get(SESSION_COOKIE)?.value
  return token ? verifySessionToken(token) : null
}

export async function GET() {
  const session = await getSession()
  if (!session?.isSuperAdmin) {
    return NextResponse.json({ error: 'Unauthorized.' }, { status: 401 })
  }

  const supabase = createAdminClient()
  const { data, error } = await supabase
    .from('groups')
    .select('id, name, is_system, created_at')
    .order('name', { ascending: true })

  if (error) return NextResponse.json({ error: 'Failed to fetch groups.' }, { status: 500 })
```

#### `app/api/dashboard/users/route.ts`
```
import { NextResponse } from 'next/server'
import { createAdminClient } from '@/lib/supabase'
import { verifySessionToken, SESSION_COOKIE } from '@/lib/session'
import { cleanField, isValidEmail, LIMITS } from '@/lib/form-utils'
import { cookies } from 'next/headers'

async function getSession() {
  const cookieStore = await cookies()
  const token = cookieStore.get(SESSION_COOKIE)?.value
  return token ? verifySessionToken(token) : null
}

export async function GET() {
  const session = await getSession()
  if (!session?.isSuperAdmin) {
    return NextResponse.json({ error: 'Unauthorized.' }, { status: 401 })
  }

  const supabase = createAdminClient()
  const { data, error } = await supabase
    .from('users')
    .select('id, email, is_super_admin, group_id, created_at, groups(name)')
    .order('created_at', { ascending: true })

  if (error) return NextResponse.json({ error: 'Failed to fetch users.' }, { status: 500 })
```

#### `app/api/dashboard/submissions/route.ts`
```
import { NextResponse } from 'next/server'
import { createAdminClient } from '@/lib/supabase'
import { verifySessionToken, SESSION_COOKIE } from '@/lib/session'
import { cookies } from 'next/headers'

export async function GET(request: Request) {
  const cookieStore = await cookies()
  const token = cookieStore.get(SESSION_COOKIE)?.value
  const session = token ? await verifySessionToken(token) : null

  if (!session) {
    return NextResponse.json({ error: 'Unauthorized.' }, { status: 401 })
  }

  const { searchParams } = new URL(request.url)
  const formType = searchParams.get('form_type')
  const page = Math.max(1, parseInt(searchParams.get('page') ?? '1'))
  const limit = 20
  const offset = (page - 1) * limit

  const supabase = createAdminClient()
  let query = supabase
    .from('submissions')
    .select('*', { count: 'exact' })
    .order('created_at', { ascending: false })
```

#### `app/api/dashboard/services/route.ts`
```
import { NextResponse } from 'next/server'
import { createAdminClient } from '@/lib/supabase'
import { verifySessionToken, SESSION_COOKIE } from '@/lib/session'
import { cleanField, LIMITS } from '@/lib/form-utils'
import { cookies } from 'next/headers'
import { revalidateTag } from 'next/cache'

async function getSession() {
  const cookieStore = await cookies()
  const token = cookieStore.get(SESSION_COOKIE)?.value
  return token ? verifySessionToken(token) : null
}

async function canManageServices(session: Awaited<ReturnType<typeof getSession>>) {
  if (!session) return false
  if (session.isSuperAdmin) return true
  const supabase = createAdminClient()
  const { data } = await supabase
    .from('users')
    .select('group_id, groups(name)')
    .eq('id', session.userId)
    .single()
  return (data?.groups as { name: string } | null)?.name === 'Marketing'
}

```

#### `app/api/dashboard/services/[id]/route.ts`
```
import { NextResponse } from 'next/server'
import { createAdminClient } from '@/lib/supabase'
import { verifySessionToken, SESSION_COOKIE } from '@/lib/session'
import { cleanField, LIMITS } from '@/lib/form-utils'
import { cookies } from 'next/headers'
import { revalidateTag } from 'next/cache'

async function getSession() {
  const cookieStore = await cookies()
  const token = cookieStore.get(SESSION_COOKIE)?.value
  return token ? verifySessionToken(token) : null
}

async function canManageServices(session: Awaited<ReturnType<typeof getSession>>) {
  if (!session) return false
  if (session.isSuperAdmin) return true
  const supabase = createAdminClient()
  const { data } = await supabase
    .from('users')
    .select('group_id, groups(name)')
    .eq('id', session.userId)
    .single()
  return (data?.groups as { name: string } | null)?.name === 'Marketing'
}

```

#### `app/api/dashboard/services/[id]/content/route.ts`
```
import { NextResponse } from 'next/server'
import { createAdminClient } from '@/lib/supabase'
import { verifySessionToken, SESSION_COOKIE } from '@/lib/session'
import { cookies } from 'next/headers'
import { revalidateTag } from 'next/cache'

async function getSession() {
  const cookieStore = await cookies()
  const token = cookieStore.get(SESSION_COOKIE)?.value
  return token ? verifySessionToken(token) : null
}

async function canManageServices(session: Awaited<ReturnType<typeof getSession>>) {
  if (!session) return false
  if (session.isSuperAdmin) return true
  const supabase = createAdminClient()
  const { data } = await supabase
    .from('users')
    .select('group_id, groups(name)')
    .eq('id', session.userId)
    .single()
  return (data?.groups as { name: string } | null)?.name === 'Marketing'
}

// PUT — upsert content for a section
```


### Database query sites (30)
For each, judge whether ownership filtering (user_id / org_id / etc.) is applied.

- `components/hero.tsx:9` — `.from('services')`
- `components/about-story.tsx:9` — `.from('services')`
- `components/services-list.tsx:26` — `.from('services')`
- `components/profile-overview.tsx:10` — `.from('services')`
- `components/book-fleet.tsx:53` — `src="https://fwndqprdqitzrprauvqy.supabase.co/storage/v1/object/public/brand-assets/photos/Jexpress%20Tourist%20Transport%20Cooperative/1775101309773-472336187_122131206134396609_331734107237458708_n.`
- `components/book-fleet.tsx:61` — `src="https://fwndqprdqitzrprauvqy.supabase.co/storage/v1/object/public/brand-assets/photos/Jexpress%20Tourist%20Transport%20Cooperative/1775101309773-472318464_122131383872396609_3251099583690664541_n`
- `components/book-fleet.tsx:69` — `src="https://fwndqprdqitzrprauvqy.supabase.co/storage/v1/object/public/brand-assets/photos/Jexpress%20Tourist%20Transport%20Cooperative/1775101309773-472257212_122131383326396609_1045635834548108228_n`
- `components/footer.tsx:20` — `.from('services')`
- `app/dashboard/(protected)/layout.tsx:29` — `.from('users')`
- `app/dashboard/(protected)/services/page.tsx:19` — `.from('users')`
- `app/api/contact/route.ts:36` — `await supabase.from('submissions').insert({`
- `app/api/inquiry/route.ts:37` — `await supabase.from('submissions').insert({`
- `app/api/booking/route.ts:40` — `await supabase.from('submissions').insert({`
- `app/api/franchise/route.ts:33` — `await supabase.from('submissions').insert({`
- `app/api/services/route.ts:10` — `.from('services')`
- `app/api/dashboard/verify-otp/route.ts:27` — `.from('users')`
- `app/api/dashboard/verify-otp/route.ts:38` — `.from('otp_tokens')`
- `app/api/dashboard/verify-otp/route.ts:53` — `await supabase.from('otp_tokens').update({ used: true }).eq('id', tokenRow.id)`
- `app/api/dashboard/request-otp/route.ts:37` — `.from('users')`
- `app/api/dashboard/request-otp/route.ts:54` — `.from('otp_tokens')`
- `app/api/dashboard/request-otp/route.ts:62` — `const { error: insertError } = await supabase.from('otp_tokens').insert({`
- `app/api/dashboard/groups/route.ts:21` — `.from('groups')`
- `app/api/dashboard/groups/route.ts:47` — `.from('groups')`
- `app/api/dashboard/groups/route.ts:76` — `.from('groups')`
- `app/api/dashboard/groups/route.ts:85` — `await supabase.from('groups').delete().eq('id', id)`
- `app/api/dashboard/users/route.ts:21` — `.from('users')`
- `app/api/dashboard/users/route.ts:51` — `.from('users')`
- `app/api/dashboard/users/route.ts:79` — `const { data: user } = await supabase.from('users').select('is_super_admin').eq('id', id).single()`
- `app/api/dashboard/users/route.ts:84` — `await supabase.from('users').delete().eq('id', id)`
- `app/api/dashboard/users/route.ts:106` — `.from('users')`


### File-upload sites (62)
For each, judge whether server-side MIME type and size validation exists.

- `components/book-form.tsx:30` — `const [formData, setFormData] = useState({`
- `components/book-form.tsx:77` — `if (!formData.fullName.trim()) errs.fullName = 'Full name is required.'`
- `components/book-form.tsx:78` — `if (!formData.contactNumber.trim()) errs.contactNumber = 'Contact number is required.'`
- `components/book-form.tsx:79` — `if (!formData.email.trim()) {`
- `components/book-form.tsx:81` — `} else if (!isValidEmail(formData.email.trim())) {`
- `components/book-form.tsx:84` — `if (!formData.serviceNeeded) errs.serviceNeeded = 'Please select a service.'`
- `components/book-form.tsx:102` — `body: JSON.stringify(formData),`
- `components/book-form.tsx:230` — `value={formData.fullName}`
- `components/book-form.tsx:245` — `value={formData.companyName}`
- `components/book-form.tsx:262` — `value={formData.contactNumber}`
- `components/book-form.tsx:277` — `value={formData.email}`
- `components/book-form.tsx:293` — `value={formData.serviceNeeded}`
- `components/book-form.tsx:313` — `value={formData.tripDate}`
- `components/book-form.tsx:330` — `value={formData.pickupLocation}`
- `components/book-form.tsx:344` — `value={formData.dropoffLocation}`
- `components/book-form.tsx:362` — `value={formData.numberOfPassengers}`
- `components/book-form.tsx:374` — `value={formData.preferredVehicle}`
- `components/book-form.tsx:398` — `value={formData.additionalNotes}`
- `components/franchise-form.tsx:21` — `const [formData, setFormData] = useState({`
- `components/franchise-form.tsx:41` — `if (!formData.fullName.trim()) newErrors.fullName = 'Full name is required.'`
- `components/franchise-form.tsx:42` — `if (!formData.contactNumber.trim()) newErrors.contactNumber = 'Contact number is required.'`
- `components/franchise-form.tsx:43` — `if (!formData.email.trim()) {`
- `components/franchise-form.tsx:45` — `} else if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(formData.email)) {`
- `components/franchise-form.tsx:65` — `body: JSON.stringify(formData),`
- `components/franchise-form.tsx:142` — `value={formData.fullName}`
- `components/franchise-form.tsx:159` — `value={formData.contactNumber}`
- `components/franchise-form.tsx:179` — `value={formData.email}`
- `components/franchise-form.tsx:196` — `value={formData.preferredLocation}`
- `components/franchise-form.tsx:212` — `value={formData.message}`
- `components/contact.tsx:27` — `const [formData, setFormData] = useState({`
- `components/contact.tsx:74` — `if (!formData.fullName.trim()) errs.fullName = 'Full name is required.'`
- `components/contact.tsx:75` — `if (!formData.contactNumber.trim()) errs.contactNumber = 'Contact number is required.'`
- `components/contact.tsx:76` — `if (!formData.email.trim()) {`
- `components/contact.tsx:78` — `} else if (!isValidEmail(formData.email.trim())) {`
- `components/contact.tsx:81` — `if (!formData.serviceNeeded) errs.serviceNeeded = 'Please select a service.'`
- `components/contact.tsx:99` — `body: JSON.stringify(formData),`
- `components/contact.tsx:267` — `value={formData.fullName}`
- `components/contact.tsx:283` — `value={formData.companyName}`
- `components/contact.tsx:301` — `value={formData.contactNumber}`
- `components/contact.tsx:317` — `value={formData.email}`
- `components/contact.tsx:334` — `value={formData.serviceNeeded}`
- `components/contact.tsx:355` — `value={formData.tripDate}`
- `components/contact.tsx:373` — `value={formData.pickupLocation}`
- `components/contact.tsx:388` — `value={formData.dropoffLocation}`
- `components/contact.tsx:407` — `value={formData.numberOfPassengers}`
- `components/contact.tsx:420` — `value={formData.preferredVehicle}`
- `components/contact.tsx:445` — `value={formData.additionalNotes}`
- `components/contact-form.tsx:18` — `const [formData, setFormData] = useState({`
- `components/contact-form.tsx:53` — `if (!formData.fullName.trim()) errs.fullName = 'Full name is required.'`
- `components/contact-form.tsx:54` — `if (!formData.email.trim()) {`
- `components/contact-form.tsx:56` — `} else if (!isValidEmail(formData.email.trim())) {`
- `components/contact-form.tsx:59` — `if (!formData.inquiryType) errs.inquiryType = 'Please select an inquiry type.'`
- `components/contact-form.tsx:60` — `if (!formData.message.trim()) errs.message = 'Message is required.'`
- `components/contact-form.tsx:78` — `body: JSON.stringify(formData),`
- `components/contact-form.tsx:195` — `value={formData.fullName}`
- `components/contact-form.tsx:210` — `value={formData.email}`
- `components/contact-form.tsx:228` — `value={formData.phone}`
- `components/contact-form.tsx:242` — `value={formData.organization}`
- `components/contact-form.tsx:257` — `value={formData.inquiryType}`
- `components/contact-form.tsx:280` — `value={formData.passengers}`
- `components/contact-form.tsx:295` — `value={formData.preferredDate}`
- `components/contact-form.tsx:310` — `value={formData.message}`


### Form handlers (13)
For each, judge: client-side schema validation present? Server-side validation on the submit
endpoint? Input sanitization before any rendering or storage? No dangerous sink (eval, dangerouslySetInnerHTML,
SQL concat, deep-link constructed from input) for unsanitized input? Rate limiting / captcha on public-facing
forms (signup, password reset, contact)?

#### `components/book-form.tsx` — form-library, text-input
```
'use client'

import { useState, useEffect } from 'react'
import { Send, CheckCircle, XCircle, Loader2 } from 'lucide-react'

type FormState = 'idle' | 'loading' | 'success' | 'error'
type FormErrors = Partial<Record<'fullName' | 'contactNumber' | 'email' | 'serviceNeeded', string>>

interface ServiceItem {
  name: string
}

function isValidEmail(email: string) {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)
}

const inputClass =
  'w-full bg-white border border-[#d1d5db] focus:border-[#d4a53a] focus:ring-2 focus:ring-[#d4a53a]/20 text-[#383838] placeholder:text-[#9ca3af] rounded-xl px-4 py-3.5 text-sm font-inter outline-none transition-all duration-200'

const labelClass =
  'text-[#383838] text-xs font-bold font-inter uppercase tracking-wide mb-1.5 block'
const inputErrorClass =
  'w-full bg-white border border-red-400 focus:border-red-400 focus:ring-2 focus:ring-red-400/20 text-[#383838] placeholder:text-[#9ca3af] rounded-xl px-4 py-3.5 text-sm font-inter outline-none transition-all duration-200'

export default function BookForm() {
  const [formState, setFormState] = useState<FormState>('idle')
  const [errorMessage, setErrorMessage] = useState('')
  const [errors, setErrors] = useState<FormErrors>({})
  const [serviceNames, setServiceNames] = useState<string[]>([])
  const [formData, setFormData] = useState({
```

#### `components/franchise-form.tsx` — form-library, text-input
```
'use client'

import { useState } from 'react'
import { Send, CheckCircle, XCircle, Loader2 } from 'lucide-react'

type FormState = 'idle' | 'loading' | 'success' | 'error'

const inputClass =
  'w-full bg-white border border-[#d1d5db] focus:border-[#d4a53a] focus:ring-2 focus:ring-[#d4a53a]/20 text-[#383838] placeholder:text-[#9ca3af] rounded-xl px-4 py-3.5 text-sm font-inter outline-none transition-all duration-200'

const inputErrorClass =
  'w-full bg-white border border-red-400 focus:border-red-400 focus:ring-2 focus:ring-red-400/20 text-[#383838] placeholder:text-[#9ca3af] rounded-xl px-4 py-3.5 text-sm font-inter outline-none transition-all duration-200'

const labelClass =
  'text-[#383838] text-xs font-bold font-inter uppercase tracking-wide mb-1.5 block'

export default function FranchiseForm() {
  const [formState, setFormState] = useState<FormState>('idle')
  const [errorMessage, setErrorMessage] = useState('')
  const [errors, setErrors] = useState<{ fullName?: string; contactNumber?: string; email?: string }>({})
  const [formData, setFormData] = useState({
    fullName: '',
    contactNumber: '',
    email: '',
    preferredLocation: '',
    message: '',
  })

  const handleChange = (
    e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>
```

#### `components/contact.tsx` — form-library, text-input
```
'use client'

import { useState, useEffect } from 'react'
import {
  RiMapPinLine,
  RiPhoneLine,
  RiMailLine,
  RiSendPlaneLine,
  RiCheckLine,
  RiCloseLine,
} from 'react-icons/ri'
import FormField from '@/components/form-field'
import { extensionSafeFormProps, extensionSafeInputProps } from '@/lib/form-extension-guard'

type FormState = 'idle' | 'loading' | 'success' | 'error'
type FormErrors = Partial<Record<'fullName' | 'contactNumber' | 'email' | 'serviceNeeded', string>>

function isValidEmail(email: string) {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)
}

export default function Contact() {
  const [formState, setFormState] = useState<FormState>('idle')
  const [errorMessage, setErrorMessage] = useState('')
  const [errors, setErrors] = useState<FormErrors>({})
  const [serviceNames, setServiceNames] = useState<string[]>([])
  const [formData, setFormData] = useState({
    fullName: '',
    companyName: '',
    contactNumber: '',
```

#### `components/franchise-login.tsx` — text-input
```
'use client'

import { useState } from 'react'
import { useRouter, useSearchParams } from 'next/navigation'
import Image from 'next/image'
import { RiLockLine, RiArrowRightLine, RiEyeLine, RiEyeOffLine, RiLoader4Line } from 'react-icons/ri'

export default function FranchiseLogin() {
  const [password, setPassword] = useState('')
  const [showPassword, setShowPassword] = useState(false)
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)
  const router = useRouter()
  const searchParams = useSearchParams()

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setLoading(true)
    setError('')

    try {
      const res = await fetch('/api/franchise-auth', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ password }),
      })

      if (res.ok) {
        const from = searchParams.get('from') || '/franchise'
        router.push(from)
```

#### `components/contact-form.tsx` — form-library, text-input
```
'use client'

import { useState, useEffect } from 'react'
import { Send, CheckCircle, XCircle, Loader2 } from 'lucide-react'

type FormState = 'idle' | 'loading' | 'success' | 'error'
type FormErrors = Partial<Record<'fullName' | 'email' | 'inquiryType' | 'message', string>>

function isValidEmail(email: string) {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)
}

export default function ContactForm() {
  const [formState, setFormState] = useState<FormState>('idle')
  const [errorMessage, setErrorMessage] = useState('')
  const [errors, setErrors] = useState<FormErrors>({})
  const [serviceNames, setServiceNames] = useState<string[]>([])
  const [formData, setFormData] = useState({
    fullName: '',
    email: '',
    phone: '',
    organization: '',
    inquiryType: '',
    passengers: '',
    preferredDate: '',
    message: '',
  })

  useEffect(() => {
    fetch('/api/services')
```

#### `components/ui/input-group.tsx` — text-input
```
'use client'

import { cva, type VariantProps } from 'class-variance-authority'

import { cn } from '@/lib/utils'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Textarea } from '@/components/ui/textarea'

function InputGroup({ className, ...props }: React.ComponentProps<'div'>) {
  return (
    <div
      data-slot="input-group"
      role="group"
      className={cn(
        'group/input-group border-input dark:bg-input/30 relative flex w-full items-center rounded-md border shadow-xs transition-[color,box-shadow] outline-none',
        'h-9 has-[>textarea]:h-auto',

        // Variants based on alignment.
        'has-[>[data-align=inline-start]]:[&>input]:pl-2',
        'has-[>[data-align=inline-end]]:[&>input]:pr-2',
        'has-[>[data-align=block-start]]:h-auto has-[>[data-align=block-start]]:flex-col has-[>[data-align=block-start]]:[&>input]:pb-3',
        'has-[>[data-align=block-end]]:h-auto has-[>[data-align=block-end]]:flex-col has-[>[data-align=block-end]]:[&>input]:pt-3',

        // Focus state.
        'has-[[data-slot=input-group-control]:focus-visible]:border-ring has-[[data-slot=input-group-control]:focus-visible]:ring-ring/50 has-[[data-slot=input-group-control]:focus-visible]:ring-[3px]',

        // Error state.
        'has-[[data-slot][aria-invalid=true]]:ring-destructive/20 has-[[data-slot][aria-invalid=true]]:border-destructive dark:has-[[data-slot][aria-invalid=true]]:ring-destructive/40',

```

#### `components/ui/sidebar.tsx` — text-input
```
'use client'

import * as React from 'react'
import { Slot } from '@radix-ui/react-slot'
import { cva, VariantProps } from 'class-variance-authority'
import { PanelLeftIcon } from 'lucide-react'

import { useIsMobile } from '@/hooks/use-mobile'
import { cn } from '@/lib/utils'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Separator } from '@/components/ui/separator'
import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetHeader,
  SheetTitle,
} from '@/components/ui/sheet'
import { Skeleton } from '@/components/ui/skeleton'
import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from '@/components/ui/tooltip'

const SIDEBAR_COOKIE_NAME = 'sidebar_state'
const SIDEBAR_COOKIE_MAX_AGE = 60 * 60 * 24 * 7
const SIDEBAR_WIDTH = '16rem'
```

#### `components/ui/input.tsx` — text-input
```
import * as React from 'react'

import { cn } from '@/lib/utils'

function Input({ className, type, ...props }: React.ComponentProps<'input'>) {
  return (
    <input
      type={type}
      data-slot="input"
      className={cn(
        'file:text-foreground placeholder:text-muted-foreground selection:bg-primary selection:text-primary-foreground dark:bg-input/30 border-input h-9 w-full min-w-0 rounded-md border bg-transparent px-3 py-1 text-base shadow-xs transition-[color,box-shadow] outline-none file:inline-flex file:h-7 file:border-0 file:bg-transparent file:text-sm file:font-medium disabled:pointer-events-none disabled:cursor-not-allowed disabled:opacity-50 md:text-sm',
        'focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px]',
        'aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive',
        className,
      )}
      {...props}
    />
  )
}

export { Input }
```

#### `components/ui/form.tsx` — form-library
```
'use client'

import * as React from 'react'
import * as LabelPrimitive from '@radix-ui/react-label'
import { Slot } from '@radix-ui/react-slot'
import {
  Controller,
  FormProvider,
  useFormContext,
  useFormState,
  type ControllerProps,
  type FieldPath,
  type FieldValues,
} from 'react-hook-form'

import { cn } from '@/lib/utils'
import { Label } from '@/components/ui/label'

const Form = FormProvider

type FormFieldContextValue<
  TFieldValues extends FieldValues = FieldValues,
  TName extends FieldPath<TFieldValues> = FieldPath<TFieldValues>,
> = {
  name: TName
}

const FormFieldContext = React.createContext<FormFieldContextValue>(
  {} as FormFieldContextValue,
)
```

#### `components/dashboard/services-manager.tsx` — text-input
```
'use client'

import { useState, useEffect, useCallback } from 'react'
import {
  RiAddLine, RiLoader4Line, RiEditLine, RiEyeLine, RiEyeOffLine,
  RiArrowUpLine, RiArrowDownLine, RiDeleteBinLine, RiCheckLine,
  RiCloseLine, RiPriceTag3Line,
} from 'react-icons/ri'

import ServiceIconPicker from '@/components/dashboard/service-icon-picker'

interface ServiceContent {
  id?: string
  section_key: string
  description: string
  tags: string[]
}

interface Service {
  id: string
  slug: string
  name: string
  price: number | null
  price_label: 'flat' | 'from'
  icon: string
  sort_order: number
  is_active: boolean
  service_content: ServiceContent[]
}

```

#### `components/dashboard/user-management.tsx` — text-input
```
'use client'

import { useState, useEffect, useCallback } from 'react'
import { RiAddLine, RiDeleteBinLine, RiLoader4Line, RiUserLine, RiGroupLine, RiLockLine } from 'react-icons/ri'

interface Group {
  id: string
  name: string
  is_system: boolean
}

interface User {
  id: string
  email: string
  is_super_admin: boolean
  group_id: string | null
  created_at: string
  groups: { name: string } | null
}

export default function UserManagement() {
  const [users, setUsers] = useState<User[]>([])
  const [groups, setGroups] = useState<Group[]>([])
  const [loading, setLoading] = useState(true)

  const [newUserEmail, setNewUserEmail] = useState('')
  const [newUserGroup, setNewUserGroup] = useState('')
  const [addingUser, setAddingUser] = useState(false)
  const [userError, setUserError] = useState('')

```

#### `components/dashboard/submissions-table.tsx` — text-input
```
'use client'

import { useState, useEffect, useCallback } from 'react'
import { RiSearchLine, RiArrowDownSLine, RiArrowUpSLine, RiRefreshLine } from 'react-icons/ri'

interface Submission {
  id: string
  form_type: string
  data: Record<string, string>
  created_at: string
}

const FORM_TYPE_LABELS: Record<string, string> = {
  contact: 'Contact',
  booking: 'Booking',
  inquiry: 'Inquiry',
  franchise: 'Franchise',
}

const FORM_TYPE_COLORS: Record<string, string> = {
  contact:   'bg-blue-100 text-blue-700',
  booking:   'bg-green-100 text-green-700',
  inquiry:   'bg-purple-100 text-purple-700',
  franchise: 'bg-orange-100 text-[#d4a53a]',
}

function formatDate(iso: string) {
  return new Date(iso).toLocaleString('en-PH', {
    month: 'short', day: 'numeric', year: 'numeric',
    hour: '2-digit', minute: '2-digit',
```

#### `components/dashboard/login-form.tsx` — text-input
```
'use client'

import { useState, useRef } from 'react'
import { RiMailLine, RiArrowRightLine, RiLoader4Line, RiShieldCheckLine } from 'react-icons/ri'

type Step = 'email' | 'otp'

export default function DashboardLoginForm() {
  const [step, setStep] = useState<Step>('email')
  const [email, setEmail] = useState('')
  const [otp, setOtp] = useState('')
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState('')
  const [resendCooldown, setResendCooldown] = useState(0)
  const cooldownRef = useRef<ReturnType<typeof setInterval> | null>(null)

  const inputClass =
    'w-full bg-[#f5f5f5]/10 border border-white/20 focus:border-[#d4a53a] focus:ring-2 focus:ring-[#d4a53a]/20 text-white placeholder:text-white/30 rounded-xl px-4 py-3.5 text-sm font-inter outline-none transition-all duration-200'

  function startCooldown() {
    setResendCooldown(60)
    cooldownRef.current = setInterval(() => {
      setResendCooldown((v) => {
        if (v <= 1) {
          clearInterval(cooldownRef.current!)
          return 0
        }
        return v - 1
      })
    }, 1000)
```


---

## PASSED CHECKS (deterministic)
- No hardcoded secrets matched known provider patterns or generic credential assignments.
- No TypeScript files have excessive `any` usage (threshold: 5).
- No URL strings contain confirmed/declared sensitive field names.
- No AsyncStorage usage of fields requiring SecureStore/Keychain (per project-context).
- No analytics/error-reporter calls leak fields requiring scrubbing (per project-context).
- No misconfiguration hits: no hardcoded localhost URLs, no `debug: true` flags outside __DEV__ gates, no NSAllowsArbitraryLoads, no Android cleartext traffic enabled.
- Lockfile present — dependency versions are pinned.
- No dangerous platform permissions requested (Info.plist, AndroidManifest.xml, app.json).


---

## PROJECT-CONTEXT REMINDERS

These are declared in project-context.md and are not statically verifiable. Surface them
to the developer as items needing manual / runtime confirmation per audit.

### USER_ROLES
- `user` — default, self-data access only (Row-Level Security enforced)
- `admin` — full data access, audit log access
- `support` — read-only access to user data for support tickets


### COMPLIANCE_REQUIREMENTS
- GDPR — EU users have right to export and right to deletion
- Apple App Store Privacy Labels — must be completed before first TestFlight
- Google Play Data Safety Form — must be completed before first Play Console
- PCI-DSS — if handling card data
- HIPAA — if handling health data
- SOC 2 — if handling enterprise customer data


### HIGH_RISK_FEATURES
- Auth flow — login, signup, password reset, OAuth, SSO
- Payment processing
- File uploads from users
- Deep links / URL schemes / Universal Links
- WebView usage
- Third-party SDK integrations (analytics, crash reporters, payment processors)
- Background location, camera, microphone, contacts access


---

## SUGGESTED AUDIT HISTORY ENTRY for project-context.md

Append this line to the AUDIT HISTORY table in project-context.md once findings are triaged:

| 2026-05-28 | baseline | 5 Critical · 43 High · 24 Medium | _(pending verdict)_ |
